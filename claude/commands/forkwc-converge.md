---
description: Converge the working diff to a fixed point in a fork that inherits the conversation — the rounds and the edit churn die there, only the net comes back
---

Spawn one fork with the Agent tool (`subagent_type: "fork"`) and prompt it: *Invoke `/converge-loop` and follow it.* Nothing more — it inherits this conversation, so the plan, the steers, and everything tried and rejected are already in its hands.

Dispatching is the work you were given, not a way of passing it on. Don't run the rounds yourself and don't fetch the diff. When the fork returns, relay its net.
