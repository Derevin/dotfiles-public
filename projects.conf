# repo-basename = project-name
# Maps a repo directory's basename to a project name (used by find-project.sh
# and the task scripts). Default when unlisted is the lowercased basename.
# Template — add your own mappings below.
dotfiles = dotfiles
