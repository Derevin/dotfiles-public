# Minimal tmux config for popup inner sessions — reduce ConPTY overhead
source-file ~/.tmux-base.conf
set -g prefix None
unbind C-b
set -g status off
set -g destroy-unattached on

bind -n M-w run-shell '~/.local/bin/cc-save-editor.sh ; tmux kill-session'
bind -n M-v copy-mode
